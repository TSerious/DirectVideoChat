import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'open.direct.video',
    appName: 'directvideo',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
